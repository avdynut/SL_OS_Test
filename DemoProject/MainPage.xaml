<UserControl x:Class="DemoProject.MainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:VirtuosoCoreControls="clr-namespace:Virtuoso.Core.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
             d:DesignHeight="300"
             d:DesignWidth="400"
             mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <VirtuosoCoreControls:DisciplineMultiSelectCombo Grid.Row="0"
                                                         Grid.Column="1"
                                                         Height="50"
                                                         Margin="4"
                                                         DisplayMemberPath="Description"
                                                         IncludeAll="true"
                                                         ItemsSource="{Binding AllDisciplines, Mode=OneTime}"
                                                         SelectedKeys="{Binding SelectedDisciplines, Mode=TwoWay}"
                                                         SelectedValuePath="DisciplineKey">
            <VirtuosoCoreControls:DisciplineMultiSelectCombo.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Description, Converter={StaticResource NullToAllConverter}}"/>
                </DataTemplate>
            </VirtuosoCoreControls:DisciplineMultiSelectCombo.ItemTemplate>
        </VirtuosoCoreControls:DisciplineMultiSelectCombo>
    </Grid>
</UserControl>
